find_package(Boost REQUIRED)
add_executable(elc-cc coreclient/main.cpp)
include_directories(${Boost_INCLUDE_DIRS})
target_link_libraries(elc-cc univalue ${Boost_LIBRARIES})
add_subdirectory(univalue)
